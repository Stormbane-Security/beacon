server {
    listen 80;
    location /redirect {
        if ($arg_url) {
            return 302 $arg_url;
        }
        return 200 "no url param";
    }
    location / {
        return 200 "home";
    }
}

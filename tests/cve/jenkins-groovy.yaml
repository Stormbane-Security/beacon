name: jenkins-groovy-rce
description: "Jenkins with exposed Groovy script console — beacon detects unauthenticated RCE surface"
tags: [cve, jenkins, rce, authorized]

services:
  jenkins:
    image: jenkins/jenkins:2.346.3-lts
    ports: ["9271:8080"]
    environment:
      JAVA_OPTS: "-Djenkins.install.runSetupWizard=false"

ready:
  cmd: "curl -sf http://localhost:9271/login >/dev/null"
  timeout: 180s
  interval: 5s

assertions:
  - name: detects-jenkins-cicd-panel
    type: beacon
    target: localhost:9271
    args: ["--no-enrich", "--no-nmap", "--scanners", "jenkins", "--deep", "--permission-confirmed", "--authorized", "--yes"]
    expect:
      check_id: exposure.cicd_panel

  - name: detects-jenkins-service
    type: beacon
    target: localhost:9271
    args: ["--no-enrich", "--no-nmap", "--scanners", "portscan", "--deep", "--permission-confirmed", "--ports", "8080"]
    expect:
      check_id: port.service_identified

timeout: 10m

server {
    listen 80;
    server_name localhost;

    location / {
        return 200 '<html><body>Secure app</body></html>';
        add_header Content-Type text/html always;
    }

    # Login redirect that only accepts relative paths (no open redirect)
    location /login {
        return 302 /dashboard;
    }
}

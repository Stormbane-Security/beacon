server {
    listen 80;
    server_name _;

    # Vulnerable: missing trailing slash on location creates path traversal
    # /static../secret/passwords.txt resolves to /var/secret/passwords.txt
    location /static {
        alias /var/www/html/;
    }

    location / {
        root /var/www/html;
    }
}

server {
    listen 80;
    server_name _;

    # Vulnerable: missing trailing slash on location creates path traversal
    # /static../etc/passwd resolves to /app/../etc/passwd → /etc/passwd
    location /static {
        alias /app/;
    }

    location / {
        root /app;
    }
}

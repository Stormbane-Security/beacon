server {
    listen 80;
    server_name _;

    location /openapi.json {
        alias /data/openapi.json;
        default_type application/json;
    }

    location /api/ {
        return 200 '{"status":"ok"}';
        default_type application/json;
    }
}

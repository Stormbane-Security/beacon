server {
    listen 80;
    server_name localhost;

    location / {
        add_header Set-Cookie "session=secure123; HttpOnly; Secure; SameSite=Strict; Path=/" always;
        return 200 '<html><body>Secure cookies</body></html>';
        add_header Content-Type text/html always;
    }
}

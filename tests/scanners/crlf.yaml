name: crlf-injection
description: "Python app that reflects user input in HTTP headers without sanitization triggers web.crlf_injection"
tags: [crlf, scanner, web]

services:
  crlf-app:
    build:
      context: ./crlf
      dockerfile: Dockerfile
    ports: ["9202:8080"]

ready:
  cmd: "curl -sf http://localhost:9202/health >/dev/null"
  timeout: 30s
  interval: 2s

assertions:
  - name: exploit-crlf-header-injection
    type: command
    expect:
      command: "curl -sI 'http://localhost:9202/redirect?url=http://example.com%0d%0aX-Injected:%20true' | grep -qi 'X-Injected'"
      exit_code: 0

  - name: detects-crlf-injection
    type: beacon
    target: crlf-app:8080
    args: ["--no-enrich", "--scanners", "crlf", "--deep", "--permission-confirmed", "--ports", "8080"]
    expect:
      check_id: web.crlf_injection

timeout: 5m

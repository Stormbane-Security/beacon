server {
    listen 80;
    server_name localhost;

    location / {
        return 200 '{"status": "ok", "endpoint": "/api/v1/data"}';
        add_header Content-Type application/json;
    }

    location /login {
        return 200 '{"status": "ok"}';
        add_header Content-Type application/json;
    }
}

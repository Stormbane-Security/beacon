server {
    listen 80;
    server_name localhost;

    add_header Content-Security-Policy "script-src 'self' 'unsafe-inline' data:; style-src 'self' 'unsafe-inline'; default-src 'self'" always;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}

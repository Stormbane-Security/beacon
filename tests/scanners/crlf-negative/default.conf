server {
    listen 80;
    server_name localhost;

    location / {
        return 200 '<html><body>No redirect params</body></html>';
        add_header Content-Type text/html always;
    }

    # Redirect endpoint that uses a fixed destination, not user input
    location /redirect {
        return 301 https://example.com/;
    }
}

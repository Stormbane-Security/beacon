server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Deliberately leak an API key in response header (simulates misconfigured proxy)
    add_header X-API-Key "sk-proj-abc123def456ghi789jkl012mno345pqr678stu901vwx" always;
    add_header X-OpenAI-Key "sk-proj-test-key-leaked-in-header" always;

    location / {
        return 200 '{"status":"ok","model":"gpt-4","api_key":"sk-live-LEAKED_KEY_IN_BODY"}';
        add_header Content-Type application/json always;
    }
}

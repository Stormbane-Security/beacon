server {
    listen 80;
    server_name _;

    location / {
        default_type text/html;
        return 200 '<html><body><h1>App</h1></body></html>';
    }

    # Exposed nginx stub_status (gateway.nginx_status_exposed)
    location /nginx_status {
        stub_status;
    }
}

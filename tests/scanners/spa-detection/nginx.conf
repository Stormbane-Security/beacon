server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: serve index.html for all routes (Angular-style routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}

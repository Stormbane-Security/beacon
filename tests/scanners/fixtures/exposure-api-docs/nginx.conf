server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        index index.html;
    }

    location /swagger-ui/ {
        alias /usr/share/nginx/html/swagger-ui/;
        index index.html;
    }
}

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    location / {
        index index.html;
    }

    location /admin/ {
        index index.html;
    }

    location /backup/ {
        index index.html;
    }
}

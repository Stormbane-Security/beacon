server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        index index.html;
    }

    location /admin/ {
        alias /usr/share/nginx/html/admin/;
        index index.html;
    }
}

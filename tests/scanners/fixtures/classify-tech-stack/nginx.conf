server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    # Simulate PHP via X-Powered-By header
    add_header X-Powered-By "PHP/8.2.0";

    location / {
        index index.html;
    }
}

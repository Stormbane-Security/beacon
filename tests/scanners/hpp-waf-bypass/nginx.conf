server {
    listen 80;
    server_name _;

    location / {
        default_type text/html;
        return 200 '<html><body>id=$arg_id</body></html>';
    }

    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}

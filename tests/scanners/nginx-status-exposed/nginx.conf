server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Exposed stub_status — should be restricted to internal networks
    location /nginx_status {
        stub_status on;
        allow all;
    }
}

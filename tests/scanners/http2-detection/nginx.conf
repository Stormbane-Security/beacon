server {
    listen 443 ssl http2;
    ssl_certificate /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;
    location / { return 200 'HTTP/2 enabled'; default_type text/plain; }
    location /health { return 200 'ok'; }
}

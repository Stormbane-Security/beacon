server {
    listen 80;
    location / {
        add_header Set-Cookie "session=abc123; Path=/";
        return 200 '<html><body>App</body></html>';
        default_type text/html;
    }
    location /health { return 200 'ok'; }
}

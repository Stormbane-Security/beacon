server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Serve the .env file at /.env
    location = /.env {
        alias /usr/share/nginx/html/.env;
        default_type text/plain;
    }

    location / {
        return 404;
    }
}

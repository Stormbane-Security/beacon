name: influxdb-exposed
description: "InfluxDB 2.7 with no authentication exposes time-series data"
tags: [influxdb, database, timeseries]

services:
  influxdb:
    image: influxdb:2.7-alpine
    ports: ["9121:8086"]
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: admin
      DOCKER_INFLUXDB_INIT_PASSWORD: password123
      DOCKER_INFLUXDB_INIT_ORG: test
      DOCKER_INFLUXDB_INIT_BUCKET: default

ready:
  cmd: "curl -sf http://localhost:9121/health | grep -q pass"
  timeout: 60s
  interval: 3s

assertions:
  - name: classifies-as-influxdb
    type: classify
    target: localhost:9121
    expect:
      backend_service: InfluxDB

  - name: detects-influxdb-exposed
    type: beacon
    target: localhost:9121
    args: ["--scanners", "portscan"]
    expect:
      check_id: port.influxdb_exposed

timeout: 5m

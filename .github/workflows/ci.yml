name: CI

on:
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  ci:
    uses: stormbane-security/bosun/.github/workflows/go-ci.yml@main
    with:
      gosec-excludes: G304,G107,G101

name: CI

on:
  pull_request:
    branches: [main, dev]

jobs:
  # Build, vet, lint, test, govulncheck, gosec — fast gate on all PRs.
  ci:
    uses: stormbane-security/bosun/.github/workflows/go-ci.yml@main
    with:
      gosec-excludes: G304,G107,G101

  # Drydock e2e tests — only on dev → main PRs, after CI passes.
  drydock:
    name: "E2E: ${{ matrix.category }}"
    needs: ci
    if: github.base_ref == 'main'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        category:
          - chains
          - databases
          - services
          - scanners
          - e2e
          - cve
          - fingerprint
          - exposure
          - web-vulns
          - infrastructure
          - nonstandard-port
          - scanner-selection
          - cicd
          - identity
          - email
          - dns
          - dlp

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@fe104658747b27e96e4f7e80cd0a94068e53901d # v2.16.1
        if: runner.environment == 'github-hosted'
        with:
          egress-policy: audit

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Build beacon
        run: |
          CGO_ENABLED=1 go build -o /usr/local/bin/beacon ./cmd/beacon
          chmod +x /usr/local/bin/beacon

      - name: Install drydock
        run: |
          go install github.com/stormbane-security/drydock/cmd/drydock@latest
          cp ~/go/bin/drydock /usr/local/bin/drydock

      - name: Skip if category is empty
        id: check
        run: |
          if [ ! -d "tests/${{ matrix.category }}" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Run drydock tests
        if: steps.check.outputs.skip != 'true'
        env:
          BEACON_AUTHORIZED_ACK: "1"
        run: |
          drydock run tests/${{ matrix.category }} \
            --fixed-ports \
            --artifacts .drydock/runs 2>&1 | tee drydock-output.log

      - name: Upload test artifacts
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: drydock-${{ matrix.category }}
          path: |
            .drydock/runs/
            drydock-output.log
          retention-days: 7

  # Gate job — single required status check for branch protection.
  drydock-gate:
    name: Drydock Gate
    needs: drydock
    if: github.base_ref == 'main'
    runs-on: ubuntu-24.04
    steps:
      - name: All drydock tests passed
        run: echo "All E2E categories passed."
